services:
  db:
    image: postgres:17.4
    restart: always
    environment:
      POSTGRES_USER: lab
      POSTGRES_PASSWORD: lab
      POSTGRES_DB: labAcc
    volumes:
      - pgdataLab:/var/lib/postgresql/data
    ports:
      - "5434:5432"
volumes:
  pgdataLab:
